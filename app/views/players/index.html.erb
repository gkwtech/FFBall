<% @players.each do |player| %>
<%= player.first_name + " " + player.last_name %>
<%= player.position %>
<%= button_to "Add Player", league_player_path(@league, player), action: :update, method: :patch %>
<% end %>
